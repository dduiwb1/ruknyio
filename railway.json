{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfile": "apps/api/Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "always"
  }
}
