{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfile": "apps/web/Dockerfile",
    "context": "apps/web"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "always"
  }
}
